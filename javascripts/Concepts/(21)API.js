//                                     API

// [1] API stands for application program interface.
// [2] Fatch API provide an interface  for sending and receiving resourses
// [3] APIs provide the data of the particular subject API related to
// [4] APIs used Request and Respone objects


// [5] Application Programming Interfaces (APIs) are constructs made available in programming
 //languages to allow developers to create complex functionality more easily. They a
 //bstract more complex code away from you, providing some easier syntax to use in its place.
//



// [6] Browser APIs are built into your web browser and are able to expose data from the browser
 //and surrounding computer environment and do useful complex things with it. For example, 
 //the Web Audio API provides JavaScript constructs for manipulating audio in the browser — taking 
 //an audio track, altering its volume, applying effects to it, etc. In the background, the
 // browser is actually using some complex lower-level code (e.g. C++ or Rust) to do the 
 //actual audio processing. But again, this complexity is abstracted away from you by the API.
// [7] Third-party APIs are not built into the browser by default, and you generally have to retrieve 
 //their code and information from somewhere on the Web. For example, the Google Maps API allows 
 //you to do things like display an interactive map to your office on your website. It provides a
 //special set of constructs you can use to query the Google Maps service and return specific information.




// [8] HTTP request methods
// HTTP defines a set of request methods to indicate the desired action to be performed for a given resource. 
 //Although they can also be nouns, these request methods are sometimes referred to as HTTP verbs. Each of them
 //implements a different semantic, but some common features are shared by a group of them: e.g. a request method 
 //can be safe, idempotent, or cacheable.

// GET
// The GET method requests a representation of the specified resource. Requests using GET should only retrieve data.

// HEAD
// The HEAD method asks for a response identical to a GET request, but without the response body.

// POST
// The POST method submits an entity to the specified resource, often causing a change in state or side effects on the server.

// PUT
// The PUT method replaces all current representations of the target resource with the request content.

// DELETE
// The DELETE method deletes the specified resource.

// CONNECT
// The CONNECT method establishes a tunnel to the server identified by the target resource.

// OPTIONS
// The OPTIONS method describes the communication options for the target resource.

// TRACE
// The TRACE method performs a message loop-back test along the path to the target resource.

// PATCH
// The PATCH method applies partial modifications to a resource.